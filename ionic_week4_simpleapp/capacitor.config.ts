import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_week4_simpleapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
